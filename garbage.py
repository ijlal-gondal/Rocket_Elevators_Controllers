

          # if len(elevatorSubList) == 0:
      #     elevatorSubList.append(self.columnList[0].elevatorList[0])

    # best_elevator_scenario_1 = column_test_case_1.requestElevator(userPosition, userDirection)
    # print("SECOND ELEVATOR ID " + str(column_test_case_1.elevatorList[1].ID))
  # print("FIRST ELEVATOR ID " + str(column_test_case_1.elevatorList[0].ID))
  # print('test1', column_test_case_1.elevatorList[0].ID)
    # findElevator('UP', column_test_case_1.elevatorList, 5)

    # best_elevator = column_test_case_1.findCallButton("UP", 2)
    # print("BEST ELEVATOR ID " + str(best_elevator.ID))
  
    # column_test_case_1.findElevator("up",4)
    # column_test_case_1.elevatorList[0].currentFloor = 1

    # column_test_case_1.elevatorList[1].currentFloor = 1


    # call_button = column_test_case_1.findCallButton(10,2)


    # column_test_case_1.requestElevator(1, 'Up')


    # best_elevator = call_button.findElevator(1,"Up")

    #  callButton = CallButton()

    # elif floor != elevator.floor:
    #   elevator.door == "CLOSE"
    # callButton = CallButton(self.direction, self.floor)

      # elevator = self.findElevator(direction,floor)
      # elevator.floorRequestList.append(floor)
      # elevator.move
    
    # controller1 = Controller(2,10)
   
    # # elevator = self.setDirection(direction, elevator)
    #   def setDirection(self, direction, elevator):
 
    # if direction == "UP":
    #   elevator.direction = "UP"
    # elif direction == "DOWN":
    #   elevator.direction == "DOWN"

      # if self.direction == (UP or DOWN or Up or Down) and self.floor <= column.floorTotal
    # elevator = Elevator.CallButtonRequestList.append(direction,floor)
    # print(floor,direction)
    
    # elevator = Elevator(1,2)
    # controller = Controller(2,10)
    # controller.move(callButton.direction,elevator.ID)

    # def CallingElevator(direction, floor):
    #     if CallButton == "Pressed":
    #         self.StopList.append(User.floor,User.direction)
    #     ActivateCallButton()
    #     FindElevator()
  # def status(self, elevator, floor):

  #   if len(elevator.floorRequestList) != [] and floor != elevator.floor:
  #     elevator.status = "MOVING"
  #   elif len(elevator.CallButtonRequestList) != [] and floor != elevator.floor:
  #     elevator.status = "MOVING"
  #   elif len(elevator.floorRequestList) != [] and floor == elevator.floor:
  #     elevator.status = "STOPPED"
  #   elif len(elevator.CallButtonRequestList) != [] and floor == elevator.floor:
  #     elevator.status = "STOPPED"
  #   elif len(elevator.floorRequestList) == [] and len(elevator.CallButtonRequestList) == []:
  #     elevator.status = "IDLE"

  #     return elevator

    # if direction == elevator.direction and floor == elevator.floor and elevator.status == "STOPPED":
    # return elevator
    # def FindElevator(direction, floor):

   # print("elevator: ", elevator)
    # print("Moving :", direction)
    # return ElevatorNumber

    # hhj

        # userPosition = 9
    # userDirection = 'DOWN'
    # userRequestFloor = 2

    # best_elevator_scenario_2 = controller.requestElevator(userDirection, userPosition)
    # print("The best found elevator for scenario 2 is " + str(best_elevator_scenario_2.ID))

    # best_elevator_scenario_2 = controller.requestFloor(best_elevator_scenario_2, userRequestFloor)
    # print("The found elevator is at floor " + str(best_elevator_scenario_2.floor))
